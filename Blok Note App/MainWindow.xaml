<Window x:Class="Blok_Note_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Blok_Note_App"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
        <WrapPanel Orientation="Horizontal">
            <ItemsControl x:Name="Bas">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Margin="5" Width="250">
                            <Grid Background="LightGray">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <!-- Имя записи -->
                                    <RowDefinition/>
                                    <!-- Контент Записи -->
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Rec}" Background="Gray" VerticalAlignment="Top" />
                                <TextBlock Text="{Binding Cont}" Grid.Row="1" VerticalAlignment="Top" 
                                       TextWrapping="Wrap" />
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel></WrapPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </WrapPanel>
    </ScrollViewer>
</Window>
